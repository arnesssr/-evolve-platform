from django.urls import path, include
from . import views

app_name = 'admin'

urlpatterns = [
    # Admin dashboard URLs will be added here
    # path('', views.dashboard, name='dashboard'),
    # path('api/', include('App.admin.api.urls')),
]
