# Production environment variables (example)
# Copy these into Render → Environment and fill in the values.
# NOTE: Do NOT commit real secrets. Keep this file as an example only.

# Django
SECRET_KEY=
DEBUG=false
ALLOWED_HOSTS=evolve-platform.onrender.com

# Production Security (optional)
# SECURE_SSL_REDIRECT=true

# Database
# Use the Service URI from your provider (Aiven) and require SSL.
DATABASE_URL=postgres://username:password@host:port/database?sslmode=require
# Optional: pin a CA file if required by your environment.
# DB_SSLROOTCERT=/path/to/ca.pem

# Pesapal (production)
PESAPAL_BASE_URL=https://pay.pesapal.com/v3
PESAPAL_CONSUMER_KEY=
PESAPAL_CONSUMER_SECRET=
PESAPAL_NOTIFICATION_ID=
PESAPAL_BRANCH=
PESAPAL_SANDBOX=false

# SMSLeopard (optional)
SMSLEOPARD_ENVIRONMENT=
SMSLEOPARD_API_KEY=
SMSLEOPARD_API_SECRET=
SMSLEOPARD_ACCESS_TOKEN=

# Email (recommended)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=true
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=no-reply@evolve-platform.onrender.com
EMAIL_SUBJECT_PREFIX=[EvolvePay] 

