services:
  - type: web
    name: evolve-platform
    env: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: bash render-start.sh
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: evolve-platform.onrender.com,.onrender.com,localhost,127.0.0.1
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.13
      # Optional Gunicorn tuning via env
      - key: GUNICORN_WORKERS
        value: ""
      - key: GUNICORN_THREADS
        value: "2"
      - key: GUNICORN_TIMEOUT
        value: "120"
      # Email (set these in Render dashboard; values omitted here on purpose)
      - key: EMAIL_BACKEND
        value: django.core.mail.backends.smtp.EmailBackend
      - key: EMAIL_HOST
        value: 
      - key: EMAIL_PORT
        value: 587
      - key: EMAIL_USE_TLS
        value: true
      - key: EMAIL_HOST_USER
        value: 
      - key: EMAIL_HOST_PASSWORD
        value: 
      - key: DEFAULT_FROM_EMAIL
        value: no-reply@evolvepay.local
      # SMSLeopard (set in dashboard)
      - key: SMSLEOPARD_API_KEY
        value: 
      - key: SMSLEOPARD_API_SECRET
        value: 
      - key: SMSLEOPARD_ACCESS_TOKEN
        value: 
    # Ensure DB migrations run during deployment
    preDeployCommand: python manage.py migrate --noinput
