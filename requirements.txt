# Evolve Payments Platform - Python Dependencies
# =============================================
# IMPORTANT: Update this file whenever you add a new dependency!
# To install: pip install -r requirements.txt

# Core Framework
Django==5.2.5

# Environment Configuration
python-decouple==3.8

# Testing
pytest==8.3.2
pytest-django==4.9.0

# HTTP Requests (for API integrations)
requests==2.32.4

# Django REST Framework (for APIs)
djangorestframework==3.14.0

# Postgres driver (psycopg 3 with binary extras; supports Python 3.13)
psycopg[binary]>=3.1

# Security: Ensure setuptools is up to date (addresses CVE vulnerabilities)
setuptools>=70.0.0

# Note: When adding new dependencies:
# 1. Install the package: pip install package_name
# 2. Add it to this file with its version: package_name==x.x.x
# 3. To get the version: pip show package_name
# 4. Commit both this file and your code changes together
